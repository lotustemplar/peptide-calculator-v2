window.APP_CONFIG = {
  // Set this to your backend or serverless endpoint when ready.
  backendBaseUrl: "",

  // Optional identifier prefix used when associating installs with OneSignal users.
  onesignalExternalIdPrefix: "peptide-calculator-v2",
};
